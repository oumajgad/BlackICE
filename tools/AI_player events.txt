OMG = {			#PLAYER
			any_country = {
				limit = { tag = SOV has_country_flag = BlackICE }

			}
		}

		OMG = {			#AI
			any_country = {
				limit = { tag = SOV NOT = { has_country_flag = BlackICE } }

			}
		}